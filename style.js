{/* <script>

    window.onload = function() {
        //recupere la valeur stockée dans le stockage locale lors du chargement de la page
        var selectedOption = localStorage.getItem("selectedOption"); 

        if(selectedOption) {
            document.getElementById("selectOptions").value = selectedOption;
        }
    }
    function redirectToPage(){
        //recupere l,element select
        var selectElement = document.getElementById("selectOptions");

        //recupere  la valeur selectionnée
        var selectedValue = selectElement.options[selectElement.selectedIndex].value;

        //stocke la valeur selectionner dans le stockage locale
        localStorage.setItem("selectedValue");

        //redirige vers la page selectionnée
        window.location.href = selectedValue;


    }
</script> */}
